PyYAML>=6.0
requests>=2.31

# Optional MVP integrations. Install only when needed.
# The market extra includes Tushare, AkShare, Baostock and yfinance;
# heavier/ad-hoc research sources such as OpenBB, AData and JQData remain manual opt-ins.
# pip install -e ".[ui,storage,market,backtest,agent,dev]"
