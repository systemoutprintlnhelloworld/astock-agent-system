"""Terminal UI support package.

The first TUI slice keeps command handlers dependency-light so they can be
tested without requiring Textual. A future Textual shell can import these
handlers and widgets directly.
"""

